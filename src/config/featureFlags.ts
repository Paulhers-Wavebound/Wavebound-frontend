/**
 * Feature Flags for Landing Page Sections
 * 
 * Toggle sections on/off while building. When you're ready to show
 * a section, just change false to true.
 * 
 * This keeps everything in one place so you don't forget!
 */

export const LANDING_PAGE_SECTIONS = {
  // Enable when we have real label/team partnerships
  showTrustedByLogos: false,
  
  // Enable when we have real artist testimonials
  showTestimonials: false,
  
  // Enable when we have real user success stories with stats
  showSocialProof: false,
  
  // Enable when we have the YouTube behind-the-scenes video ready
  showVideoShowcase: false,
  
  // Comparison table section - hidden for now
  showComparisonSection: false,
};
